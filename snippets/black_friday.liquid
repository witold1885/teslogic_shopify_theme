<style>
    header#header_main {
        height: 200vh;
    }
    .banner-desktop-wrap {
        top: unset;
        bottom: 0;
        height: 100vh;
    }
    @font-face {
        font-family: 'Neue Haas Grotesk Display Pro';
        src: url({{ 'https://cdn.shopify.com/s/files/1/0611/8041/2118/files/NeueHaasDisplayMediu.ttf?v=1763466988' }}) format("truetype");
        font-weight: normal;
        font-style: normal;
    }
    .black-friday {
        background: url({{ 'black_friday_gradient.png' | asset_url }});
        background-size: cover;
        background-position-x: center;
        background-repeat: no-repeat;
        position: absolute;
        top: 0;
        z-index: 1;
        width: 100%;
        height: 100vh;
    }
    .black-friday-wrap {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 32px;
        margin-top: 106px;
    }
    .black-friday-info {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 12px;
        color: #FFFFFF;
        text-align: center;
        font-style: normal;
    }
    .black-friday-info-title {
        font-family: "Neue Haas Grotesk Display Pro";
        font-size: 96px;
        font-weight: 900;
        line-height: 112px; /* 116.667% */
        letter-spacing: 1.92px;
    }
    .black-friday-info-subtitle {
        font-family: Manrope;
        font-size: 30px;
        font-weight: 500;
        line-height: 40px; /* 133.333% */
        letter-spacing: 0.54px;
    }
    .black-friday-info-counter {
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
        overflow: hidden;
    }
    .black-friday-info-counter * {
        margin: 0;
        padding: 0;
    }
    
</style>

<style lang="scss">
    .clock-display-grp {
        width: 100%;
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;

         .number-grp {
            width: auto;
            display: block;
            height: 156px;
            overflow: hidden;

            .number-grp-wrp {
               width: 100%;
               position: relative;

               .num {
                  width: 100%;
                  position: relative;
                  height: 156px;

                  p {
                     width: auto;
                     display: table;
                     font-size: 205px;
                     line-height: 150px; //180
                     font-weight: bold;
                  }

               }

            }

         }
      }
    .clock-separator {
         width: auto;
         float: left;
         display: block;
         height: 156px;

         p {
            width: auto;
            display: table;
            font-size: 205px;
            line-height: 150px; //180
            font-weight: bold;
         }

      }
</style>

<div class="black-friday">
    <div class="black-friday-wrap">
        <div class="black-friday-info timer">
            <div class="black-friday-info-title">Black Friday</div>
            <div class="black-friday-info-subtitle">But better — 3 days of our biggest deals</div>
            <div class="black-friday-info-counter timer--clock">
                {% assign groups = 'days,hours,minutes,seconds' | split: ',' %}
                {% assign digits = 'first,second' | split: ',' %}
                {% for group in groups %}
                    <div class="{{ group }}-group clock-display-grp">
                        {% for digit in digits %}
                            <div class="{{ digit }} number-grp">
                                <div class="number-grp-wrp">
                                    {% for i in (0..9) %}
                                        <div class="num num-{{ i }}"><p>{{ i }}</p></div>
                                    {% endfor %}
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                    {% if forloop.last == false %}
                        <div class="clock-separator"><p>:</p></div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
        <div class="black-friday-items"></div>

    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/1.19.1/TweenMax.min.js"></script>

<script type="text/javascript">
    TweenLite.defaultEase = Expo.easeOut;

    initTimer("2025-11-30T23:59:59");

    function getTimeLeft(targetDate) {
        const now = new Date();
        const target = new Date(targetDate);
        let diff = Math.max(0, target - now);

        const days = Math.floor(diff / (1000 * 60 * 60 * 24));
        diff -= days * 1000 * 60 * 60 * 24;

        const hours = Math.floor(diff / (1000 * 60 * 60));
        diff -= hours * 1000 * 60 * 60;

        const minutes = Math.floor(diff / (1000 * 60));
        diff -= minutes * 1000 * 60;

        const seconds = Math.floor(diff / 1000);

        return { days, hours, minutes, seconds };
    }

    function initTimer (targetDate) {

        const timerEl = document.querySelector('.timer');

        const groups = {
            days: {
                first: timerEl.querySelector('.days-group .first'),
                second: timerEl.querySelector('.days-group .second'),
            },
            hours: {
                first: timerEl.querySelector('.hours-group .first'),
                second: timerEl.querySelector('.hours-group .second'),
            },
            minutes: {
                first: timerEl.querySelector('.minutes-group .first'),
                second: timerEl.querySelector('.minutes-group .second'),
            },
            seconds: {
                first: timerEl.querySelector('.seconds-group .first'),
                second: timerEl.querySelector('.seconds-group .second'),
            }
        };

        function animateDigit(el, val) {
            TweenMax.killTweensOf(el.querySelector('.number-grp-wrp'));
            TweenMax.to(el.querySelector('.number-grp-wrp'), 1, {
                y: -el.querySelector('.num-' + val).offsetTop
            });
        }

        function update() {
            const t = getTimeLeft(targetDate);

            const d = String(t.days).padStart(2, "0");
            const h = String(t.hours).padStart(2, "0");
            const m = String(t.minutes).padStart(2, "0");
            const s = String(t.seconds).padStart(2, "0");

            animateDigit(groups.days.first,   d[0]);
            animateDigit(groups.days.second,  d[1]);

            animateDigit(groups.hours.first,  h[0]);
            animateDigit(groups.hours.second, h[1]);

            animateDigit(groups.minutes.first, m[0]);
            animateDigit(groups.minutes.second,m[1]);

            animateDigit(groups.seconds.first, s[0]);
            animateDigit(groups.seconds.second,s[1]);

            if (t.days === 0 && t.hours === 0 && t.minutes === 0 && t.seconds === 0) {
                return; // кінець, зупиняємо
            }

            setTimeout(update, 1000);
        }

        update();
    }
</script>
